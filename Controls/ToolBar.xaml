<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="svg_editor.Controls.ToolBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:svg_editor.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

	
	<StackPanel Orientation="Horizontal"  Padding="4" Spacing="4" Background="{ThemeResource CustomAcrylicBrush}" CornerRadius="6">
		<Button  Padding="8" Command="{Binding AddPolygonCommand}">
			<FontIcon Glyph="&#xE879;" />
		</Button>
		<Button  Padding="8" Command="{Binding AddRectCommand}">
			<FontIcon Glyph="&#xE71A;"   />
		</Button>
		<Button  Padding="8" Command="{Binding AddEllipseCommand}">
			<FontIcon Glyph="&#xEA3A;"   />
		</Button>
		<Button  Padding="8" Command="{Binding AddLineCommand}">
			<FontIcon Glyph="&#xE738;" />
		</Button>
		<Button  Padding="8" Command="{Binding AddPolylineCommand}">
			<FontIcon Glyph="&#xEDFB;" />
		</Button>
		<Button  Padding="8" Command="{Binding AddPathCommand}">
			<FontIcon Glyph="&#xF003;" />
		</Button>
		<Button Margin="8, 0,0,0" Padding=" 12, 8" Style="{StaticResource AccentButtonStyle}" Content="Open" />
		<Button  Padding=" 12, 8" Style="{StaticResource AccentButtonStyle}" Content="Save" />
	</StackPanel>
</UserControl>
